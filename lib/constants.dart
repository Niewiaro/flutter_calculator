import 'package:flutter/material.dart';

const double PADDING = 35.0;
const double BUTTON_FONT_SIZE = 40.0;
const double ICON_FONT_SIZE = 60.0;
const BorderRadius BORDER_RADIUS = BorderRadius.all(Radius.circular(15));
